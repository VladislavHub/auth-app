<div class="field">
	<label for="{{name}}" class="field__label">{{label}}</label>
	<input oninput="{{action}}(this.name, this.value)" class="field__input" name="{{name}}" type="{{type}}"
		placeholder="{{placeholder}}">
</div>